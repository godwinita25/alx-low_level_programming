#include "main.h"
/**
* _isdigit - Check for digits through 0-9
* @g: The number to be checked
* Return: 1 for a character of 0 otherwise
*/

int _isdigit(int g)
{
	if (g >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
