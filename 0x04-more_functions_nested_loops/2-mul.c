#include "main.h"
/**
* mul - Multiplies two integers
* @x: The first number to multiply
* @y: The second numbver to multiply with first number
*  Return: The result of multiplcation between x param and y param
*/

int mul(int x, int y)

{
	return (x * y);
}
